defmodule PeerLearning.Utils do
  @moduledoc false
  # def load_association(response, data, view, file, schema_key) do
  #   case Ecto.assoc_loaded?(data) do
  #     true when is_list(data) ->
  #       response
  #       |> Map.put_new(
  #         schema_key,
  #         render_many(data, view, file)
  #       )

  #     true when is_map(data) ->
  #       response
  #       |> Map.put_new(
  #         schema_key,
  #         render_one(data, view, file)
  #       )

  #     _ ->
  #       response
  #   end
  # end
end
